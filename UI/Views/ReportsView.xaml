<UserControl x:Class="Himchistka.UI.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <DatePicker SelectedDate="{Binding DateFrom}" />
            <DatePicker Margin="8,0,0,0" SelectedDate="{Binding DateTo}" />
            <Button Content="Сформировать" Command="{Binding LoadReportsCommand}" />
            <Button Content="Excel" Command="{Binding ExportExcelCommand}" />
            <Button Content="PDF" Command="{Binding ExportPdfCommand}" />
        </StackPanel>

        <TabControl Grid.Row="1">
            <TabItem Header="День"><TextBlock Text="{Binding DailyReport.OrdersTotal}"/></TabItem>
            <TabItem Header="Месяц"><TextBlock Text="{Binding MonthlyReport.Revenue}"/></TabItem>
            <TabItem Header="Популярные услуги"><DataGrid ItemsSource="{Binding PopularServices}"/></TabItem>
            <TabItem Header="Клиенты"><DataGrid ItemsSource="{Binding Clients}"/></TabItem>
        </TabControl>
    </Grid>
</UserControl>
